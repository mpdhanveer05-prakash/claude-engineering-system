# Session Index

> Most recent first. Each session file: `YYYY-MM-DD-<slug>.md` (use `_` for time if multiple/day: `YYYY-MM-DD_HHMM-<slug>.md`).

| Date | Slug | Goal | Outcome | Files touched |
|------|------|------|---------|---------------|
| YYYY-MM-DD | bootstrap | Scaffold from template | done | `CLAUDE.md`, all root |

## Conventions

- Write a session log at the end of every meaningful session.
- "Meaningful" = >15 min of edits OR any architectural decision.
- Keep this index in sync — drop the oldest rows past 50 into `INDEX-ARCHIVE.md` quarterly.
